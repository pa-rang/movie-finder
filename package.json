{
    "name": "react-toy-project",
    "version": "0.1.0",
    "scripts": {
        "client": "cd client && yarn start",
        "server": "nodemon server.js",
        "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
    },
    "dependencies": {
        "axios": "^0.19.2",
        "body-parser": "^1.19.0",
        "express": "^4.17.3",
        "request": "^2.88.2"
    },
    "devDependencies": {
        "concurrently": "^4.0.1"
    }
}
